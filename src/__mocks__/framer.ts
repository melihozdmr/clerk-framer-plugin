export const addPropertyControls = jest.fn()

export enum ControlType {
    String = "string",
    Number = "number",
    Boolean = "boolean",
    Enum = "enum",
    Color = "color",
    EventHandler = "eventhandler"
} 